<%layout ('/layouts/indexBoiler')%>
<link rel="stylesheet" href="/stylesheets/index.css">
<div id="cluster-map" class="mb-3"></div>
        <div class="container">
        <div class="row">
        <% for(let c of campgrounds){ %>
            <div class="card border-0 col-md-6 col-lg-4 px-3 mb-3 ">
            <div class="card text-white bg-dark col">
                <div class="column">
                    <div class='col'>
                        <% if(c.images.length>=1){ %> 
                        <img src="<%=c.images[0].crop%>" class='img-fluid' alt="">
                        <% } else { %>
                            <img src="https://images.unsplash.com/photo-1506803034607-ba76423b765a?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=872&q=80" class='img-fluid' alt="">    
                            <% } %> 
                    </div>
                    <div class="col">
                        <div class="card-body">
                            <h5 class="card-title"><%=c.title%></h5>
                            <p class="card-text">"<%=c.description%>"</p>
                            <p class="card-text">
                            <span class="text-muted"><%=c.location%></span>    
                            </p>
                            <!-- <a href="/campgrounds/<%=c.id%>"><button class="btn btn-primary">More</button></a> -->
                        </div>
                    </div>
                </div>
            </div>
            <div class="card border-0 d-flex align-items-end mt-1">
                <a href="/campgrounds/<%=c.id%>"><button class="btn btn-primary px-3">More-></button></a>
            </div>
            </div>
            <% } %>
        </div>
    </div>
    <a href="/campgrounds/new">Add cafe</a>
    <script>
        const mapToken='<%-process.env.MAPBOX_TOKEN%>';
        const camp={features:<%-JSON.stringify(campgrounds)%>};
    </script>
    <script src="/javascripts/cluster.js"></script>